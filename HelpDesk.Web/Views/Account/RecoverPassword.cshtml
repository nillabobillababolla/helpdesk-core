@model HelpDesk.Models.ViewModels.RecoverPasswordViewModel

@{
    ViewData["Title"] = "RecoverPassword";
}

<h2>Şifre Kurtarma Ekranı</h2>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="RecoverPassword">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Email" class="control-label"></label>
                <input asp-for="Email" class="form-control" />
                <span asp-validation-for="Email" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Şifremi Unuttum" class="btn btn-default" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

